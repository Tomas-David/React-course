import allMovies from '../data'
import { useState } from 'react'

interface TypeOfMovieType{
 id: number, image: string, title: string, age :string, tags :string, description :string, category:string
}

interface MovieCardProps{
  titleName : string
}

export const MovieCard = (titleName: MovieCardProps) =>{

}